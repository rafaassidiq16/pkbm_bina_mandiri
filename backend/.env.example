# ============================================================
# FILE RAHASIA — JANGAN PERNAH DI-COMMIT KE GIT
# Salin file ini menjadi .env dan isi sesuai konfigurasi lokal
# ============================================================

# Konfigurasi Server
PORT=3009
NODE_ENV=development

# Konfigurasi Koneksi Database MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password_anda
DB_NAME=

# Secret Key untuk JWT (ganti dengan string acak yang panjang dan aman)
JWT_SECRET=pkbm_bina_mandiri_super_secret_key_2025_ganti_ini
JWT_EXPIRES_IN=7d

# Konfigurasi CORS — URL frontend yang diizinkan mengakses API
FRONTEND_URL=http://localhost:5173
